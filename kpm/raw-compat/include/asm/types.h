/* SPDX-License-Identifier: GPL-2.0 */
/*
 * The public Pixel 7 GS source tag omits this generated UAPI alias. The
 * arm64 target uses the generic definition, so keep the build-only overlay
 * explicit instead of editing the pinned source checkout.
 */
#ifndef _R0LAB_TARGET_ASM_TYPES_H
#define _R0LAB_TARGET_ASM_TYPES_H

#include <asm-generic/types.h>

#endif
