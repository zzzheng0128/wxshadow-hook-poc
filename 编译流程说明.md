# wxshadow-hook-poc 编译流程说明

> 本文档记录 KernelPatch KPM hook PoC（r0lab 项目）的完整编译流程。
> 支持 **macOS 本机编译**（推荐，已实测）和 **Linux 编译机**两种方式。

---

## 1. 目录结构

```
wxshadow-hook-poc/
├── kpm/                        # KPM 模块源码 + Makefile（r0lab.c / r0lab_raw_compat.c）
├── lab-app/                    # 测试用 Android 应用
├── scripts/                    # 编译/测试脚本
├── tools/                      # 辅助工具
├── references/                 # 外部依赖（.gitignore 已忽略，不进 git）
│   ├── KernelPatch/            # KernelPatch 0.13.3 源码（独立 git 子仓库）
│   ├── kernel-src/             # 内核头文件树（只含头文件，非完整源码）
│   │   ├── kernel-common-6.1/  #   oriole 6.1.99（include/ + arch/arm64/include/）
│   │   └── kernel-msm-4.19/    #   redfin 4.19.278（include/ + arch/arm64/include/）
│   └── arm-gnu-toolchain/      # ARM 官方 aarch64-none-elf 裸机工具链（GCC 15.3）
└── 编译流程说明.md
```

> **⚠️ `references/` 被 `.gitignore` 忽略**：KernelPatch、内核头文件、工具链都是
> 本地依赖，不 vendor 进仓库。clone 仓库后需按第 2 节重新获取。

---

## 2. 依赖准备（一次性）

> `references/` 下的三个依赖（KernelPatch、内核头文件、工具链）都被
> `.gitignore` 忽略，不进 git。clone 仓库后按本节重新获取。

### 2.1 KernelPatch 源码

```bash
cd references/
git clone https://github.com/zzzheng0128/KernelPatch.git KernelPatch
cd KernelPatch
git checkout gcc15-compat   # 含 GCC 15 兼容修复的分支（基于上游 main）
```

> **说明**：`gcc15-compat` 分支基于上游 `main`（0.13.3 之后 34 commit），
> 修复了 `selinux_hide.c` 在 GCC 15 下的 `-Wint-conversion` 错误。
> 若只想用官方版，`main` 分支即可（但需 GCC 11 等老工具链）。

### 2.2 内核头文件树（脚本自动生成）

```bash
# 一键生成 references/kernel-src/{kernel-common-6.1,kernel-msm-4.19}
sh scripts/gen_kernel_headers.sh all        # 或 oriole / redfin
```

> 脚本会：下载内核源码（shallow clone 单分支）→ `make prepare` 生成头文件 →
> 只保留 `include/` + `arch/arm64/include/` 到 `references/kernel-src/`。
> 需要 `aarch64-linux-gnu-gcc` 交叉编译器（或用 `CROSS_COMPILE=` 指定）。

> **为什么只要头文件**：`raw_compat` 层（`kpm/r0lab_raw_compat.c`）只 include
> 内核头文件，编译期依赖 `include/generated/` 里的自动生成头（`autoconf.h`、
> `asm-offsets.h`、`version.h`），不需要完整内核源码。

### 2.3 裸机工具链（仅编译 KernelPatch kpimg 需要）

```bash
# 下载 ARM 官方 aarch64-none-elf 工具链（macOS arm64 版）
# https://developer.arm.com/downloads/-/arm-gnu-toolchain-downloads
cd references/
tar -xJf arm-gnu-toolchain-15.3.rel1-darwin-arm64-aarch64-none-elf.tar.xz
mv arm-gnu-toolchain-15.3.rel1-darwin-arm64-aarch64-none-elf arm-gnu-toolchain
```

---

## 3. 设备与目标映射

| TARGET | 设备 | 内核 | mm 锁字段 |
|---|---|---|---|
| `oriole` | Pixel 6（Android 14 GKI） | 6.1.99 | `mmap_lock` |
| `redfin` | Pixel 5（Android 14 legacy msm） | 4.19.278 | `mmap_sem` |
| `panther` | Pixel 7（原始目标，已弃用） | 5.10.x | `mmap_lock` |

---

## 4. 编译 KPM 模块

### 4.1 macOS 本机编译（✅ 已实测）

> 本机 NDK 29 的 clang 是 universal binary（含 arm64 slice），Apple Silicon
> 直跑无需 Rosetta。`kpm/Makefile` 默认 NDK 路径已匹配本机。

```bash
cd wxshadow-hook-poc
make -C kpm clean && make -C kpm TARGET=oriole    # Pixel 6
make -C kpm clean && make -C kpm TARGET=redfin    # Pixel 5
```

### 4.2 Linux 编译机编译

```bash
ssh myserver
cd ~/wxshadow
make -C kpm clean && make -C kpm TARGET=oriole
make -C kpm clean && make -C kpm TARGET=redfin
```

> Linux 下 Makefile 自动改用 `aarch64-linux-gnu-gcc`（系统交叉编译器）。

### 4.3 产物

| 目标 | 产物路径 | 模块名 |
|---|---|---|
| oriole | `kpm/build/r0lab-m1-oriole.kpm` | `r0lab-m1-oriole` |
| redfin | `kpm/build/r0lab-m1-redfin.kpm` | `r0lab-m1-redfin` |

> **重要**：两个目标共用 `kpm/build/` 目录，切换目标前必须 `make clean`。

### 4.4 Makefile 关键编译参数

- **`r0lab.c`**（KPM 主逻辑，链接 KernelPatch 头文件）：
  ```
  CFLAGS = -O2 ... -ffreestanding -fno-builtin -fno-common -fno-pic -fno-pie
           -fno-stack-protector -fno-asynchronous-unwind-tables -fno-unwind-tables
           -ffixed-x18          # ★ 关键：SCS 内核必须固定 x18
  ```

- **`r0lab_raw_compat.c`**（raw compat 层，链接内核源码头文件）：
  ```
  RAW_CFLAGS = -O2 -D__KERNEL__ -DMODULE
               -DR0LAB_RAW_MM_LOCK_FIELD=<mmap_lock|mmap_sem> ... -ffixed-x18
  ```

- **链接后 objcopy 剥离内核专用 section**：
  `.altinstructions`、`.rela.altinstructions`、`.altinstr_replacement`、
  `__jump_table`、`.rela__jump_table`、`__bug_table`、`.rela__bug_table`

> **⚠️ `-ffixed-x18` 强制**：目标内核 `CONFIG_SHADOW_CALL_STACK=y`，x18 是内核
> shadow call stack 指针，溢出会导致 SP/PC alignment panic。详见 `kpm-scs-x18` skill。

---

## 5. 编译 KernelPatch kpimg（可选，仅需重新构建 KernelPatch 时）

产物是 KernelPatch 的内核镜像补丁 payload（`kpimg` / `kpimg.elf`），
与 r0lab KPM 模块是两回事。仅当你需要重新构建 KernelPatch 本体时使用。

```bash
cd references/KernelPatch/kernel
export TARGET_COMPILE=$PWD/../../arm-gnu-toolchain/bin/aarch64-none-elf-
export ANDROID=1
make kpimg     # 只编 ARM64；kpimg-x86 需 Linux gcc，macOS 上跳过
# 产物: kernel/kpimg（340KB）+ kernel/kpimg.elf
```

> **⚠️ GCC 15 兼容性**：用 `gcc15-compat` 分支（已含修复）编译即可。若用官方
> `main` 分支 + GCC 15，会遇到 `selinux_hide.c` 的 `-Wint-conversion` 错误
> （`lookup_name_with_suffix` 返回 unsigned long 需强转 `sel_write_op_fn*`）。
> 早期 0.13.3 还有两个类似问题（`sucompat.c` 缺 `#include <userd.h>`、
> `baselib.c` 缺 `memcpy`），上游 main 已修复。

---

## 6. 编译 lab-app（Android 应用）

lab-app 通过 `am start` 触发 KPM 模块命令，仅 macOS 本机编译（脚本路径是 macOS 的）。

```bash
cd wxshadow-hook-poc
export ANDROID_SDK_ROOT=$HOME/Library/Android/sdk
export ANDROID_NDK_HOME=$HOME/Library/Android/sdk/ndk/29.0.14206865
export ANDROID_BUILD_TOOLS=$HOME/Library/Android/sdk/build-tools/34.0.0
sh scripts/build_lab_app.sh
# 产物: build/lab-app/r0lab-debug.apk
```

编译步骤：javac → d8 转 dex → clang 编译 labprobe.c 为 .so → aapt2 打包 → zipalign + apksigner 签名。

---

## 7. 部署到设备

### 7.1 设备信息

| 设备 | 序列号 | lab_uid |
|---|---|---|
| P5（redfin） | `11191FDD40013H` | 10260 |
| P6（oriole） | `18201FDF6002GR` | 10285 |

### 7.2 加载 KPM 模块

```bash
kpctl --key amigo123 load r0lab-m1-redfin.kpm lab_uid=10260   # P5
kpctl --key amigo123 load r0lab-m1-oriole.kpm lab_uid=10285   # P6
```

> superkey：`amigo123`

### 7.3 触发命令（通过 lab-app）

```bash
am start -W -n dev.r0hook.lab/.MainActivity --es r0lab_command 'status'
```

### 7.4 正确卸载流程（⚠️ 避免 use-after-free panic）

```bash
# 先 workers shutdown（lab-app 触发）→ 等 workers_live=0 → 再 unload
# 直接 unload 活跃模块会触发 KP 框架 use-after-free panic
```

---

## 8. 常见坑

1. **切换 TARGET 必须 clean**：共用 `kpm/build/`，不 clean 会混 `.o`。
2. **`-ffixed-x18` 不可省略**：SCS 内核，省略会 panic。
3. **`usage` 偏移不能硬编码**：SCS=y 时 `task_struct.usage` 偏移 0x30→0x38，
   用 `stack_in_task_offset + sizeof(unsigned long)` 动态推导。
4. **卸载要先 shutdown workers**：直接 unload 触发 use-after-free。
5. **KernelPatch kpimg 编译需用 gcc15-compat 分支**：官方 `main` + GCC 15 会在 `selinux_hide.c` 报 `-Wint-conversion`。早期 0.13.3 还有 `sucompat.c`/`baselib.c` 两个问题，上游 main 已修复。
6. **`references/` 被 gitignore**：clone 后需重新拉 KernelPatch + 内核头文件。
