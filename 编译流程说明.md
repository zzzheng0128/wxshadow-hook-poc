# wxshadow-hook-poc 编译流程说明

> 本文档记录 KernelPatch KPM hook PoC（r0lab 项目）的完整编译流程，基于编译机 `myserver`（Linux，Ubuntu 22.04）实测环境整理。

---

## 1. 编译环境总览

| 项目 | 位置 / 值 |
|---|---|
| 编译主机 | `myserver`（SSH 别名，Linux Ubuntu 22.04） |
| 源码目录 | `~/wxshadow/` |
| KernelPatch 源码 | `~/wxshadow/references/KernelPatch` |
| 内核源码（oriole） | `~/kernel-src/kernel-common-6.1`（6.1.99） |
| 内核源码（redfin） | `~/kernel-src/kernel-msm-4.19`（4.19.278） |
| 交叉编译器 | `aarch64-linux-gnu-gcc`（GCC 11.4.0） |
| 交叉 objcopy | `aarch64-linux-gnu-objcopy` |

### 设备与目标映射

| TARGET | 设备 | 内核 | mm 锁字段 |
|---|---|---|---|
| `oriole` | Pixel 6（Android 14 GKI） | 6.1.99 | `mmap_lock` |
| `redfin` | Pixel 5（Android 14 legacy msm） | 4.19.278 | `mmap_sem` |
| `panther` | Pixel 7（原始目标，已弃用） | 5.10.x | `mmap_lock` |

---

## 2. 内核源码树准备（一次性）

两个内核源码树已配置完成，无需重复操作。若在新机器上重建：

```bash
cd ~/kernel-src/kernel-common-6.1     # oriole（6.1）
# 或
cd ~/kernel-src/kernel-msm-4.19       # redfin（4.19）

# 生成 .config 和 generated 头文件（关键！raw compat 层依赖这些）
make ARCH=arm64 <defconfig>           # 按设备 defconfig
make ARCH=arm64 prepare scripts       # 生成 include/generated/ 等
```

> **关键点**：`r0lab_raw_compat.c` 编译时依赖内核的 `arch/arm64/include/generated`、
> `include/generated` 等自动生成头文件（`asm-offsets.h`、`autoconf.h`、`bounds.h` 等），
> 以及 `struct mm_struct` / `pte_t` 的内核版本 ABI。因此内核源码树必须先用
> `make prepare` 生成这些文件。

当前环境已就绪：
- `~/kernel-src/kernel-common-6.1/.config` ✓
- `~/kernel-src/kernel-msm-4.19/.config` ✓
- `include/generated/` 头文件齐全 ✓

---

## 3. 编译 KPM 模块

KPM 模块的编译完全由 `kpm/Makefile` 驱动，切换目标只需改 `TARGET` 变量。

### 3.1 编译 oriole（Pixel 6）

```bash
cd ~/wxshadow
make -C kpm clean
make -C kpm TARGET=oriole
```

### 3.2 编译 redfin（Pixel 5）

```bash
cd ~/wxshadow
make -C kpm clean
make -C kpm TARGET=redfin
```

### 3.3 产物

| 目标 | 产物路径 | 模块名 |
|---|---|---|
| oriole | `kpm/build/r0lab-m1-oriole.kpm` | `r0lab-m1-oriole` |
| redfin | `kpm/build/r0lab-m1-redfin.kpm` | `r0lab-m1-redfin` |

> **重要**：两个目标共用 `kpm/build/` 目录，切换目标前必须 `make clean`，
> 否则会混入上一次目标的 `.o` 文件。

### 3.4 Makefile 关键编译参数

编译由两个源文件组成，最终 `ld -r` 链接并 objcopy 剥离内核专用 section：

- **`r0lab.c`**（KPM 主逻辑，链接 KernelPatch 头文件）：
  ```
  CFLAGS = -O2 -Wall -Wextra -Werror ... -ffreestanding -fno-builtin
           -fno-common -fno-pic -fno-pie -fno-stack-protector
           -fno-asynchronous-unwind-tables -fno-unwind-tables
           -ffixed-x18          # ★ 关键：SCS 内核必须固定 x18
  ```

- **`r0lab_raw_compat.c`**（raw compat 层，链接内核源码头文件）：
  ```
  RAW_CFLAGS = -O2 -D__KERNEL__ -DMODULE
               -DR0LAB_RAW_MM_LOCK_FIELD=<mmap_lock|mmap_sem>
               ... -ffixed-x18
  ```

- **链接后处理**（objcopy 剥离内核专用 section）：
  ```
  --remove-section=.altinstructions --remove-section=.rela.altinstructions
  --remove-section=.altinstr_replacement --remove-section=__jump_table
  --remove-section=.rela__jump_table --remove-section=__bug_table
  --remove-section=.rela__bug_table
  ```

> **⚠️ `-ffixed-x18` 是强制的**：目标内核启用了 `CONFIG_SHADOW_CALL_STACK`，
> x18 是内核 shadow call stack 指针。任何模块函数如果把 x18 当临时寄存器
> 溢出，就会踩坏影子调用栈，导致 SP/PC alignment exception 内核 panic。
> 详见 `kpm-scs-x18` skill。

---

## 4. 编译 lab-app（Android 应用）

lab-app 是测试用的 Android 应用，通过 `am start` 触发 KPM 模块命令。

> **注意**：`scripts/build_lab_app.sh` 里的 NDK/SDK 路径是 **macOS 路径**
> （`/Users/ivory/...`、`/Users/freeman/Library/...`），该脚本是给 **macOS 本机**
> 编译用的。在 Linux 编译机上需改写路径或直接在 macOS 上编译。

### macOS 本机编译（推荐）

```bash
cd <macOS 上的项目目录>
export ANDROID_SDK_ROOT=$HOME/Library/Android/sdk
export ANDROID_NDK_HOME=$HOME/Library/Android/sdk/ndk/29.0.14206865
export ANDROID_BUILD_TOOLS=$HOME/Library/Android/sdk/build-tools/35.0.0
sh scripts/build_lab_app.sh
# 产物: build/lab-app/r0lab-debug.apk
```

编译步骤（脚本内部）：
1. `javac` 编译 Java 源 → classes
2. `d8` 转 dex
3. `aarch64-linux-android23-clang` 编译 `labprobe.c` → `liblabprobe.so`
4. `aapt2` 打包 manifest → unsigned.apk
5. 塞入 dex + so，重新 zip
6. `zipalign` + `apksigner` 签名 → `r0lab-debug.apk`

---

## 5. 部署到设备

### 5.1 设备信息

| 设备 | 序列号 | lab_uid |
|---|---|---|
| P5（redfin） | `11191FDD40013H` | 10260 |
| P6（oriole） | `18201FDF6002GR` | 10285 |

### 5.2 加载 KPM 模块

```bash
# 通过 kpctl 加载（APatch 环境）
kpctl --key amigo123 load r0lab-m1-redfin.kpm lab_uid=10260   # P5
kpctl --key amigo123 load r0lab-m1-oriole.kpm lab_uid=10285   # P6
```

> **superkey**：`amigo123`（KP supercall 密钥）

### 5.3 触发命令（通过 lab-app）

```bash
am start -W -n dev.r0hook.lab/.MainActivity \
  --es r0lab_command 'status'
```

### 5.4 正确卸载流程（⚠️ 避免 use-after-free panic）

```bash
# 先让 workers 优雅退出，等 workers_live=0，再 unload
# 直接 unload 有活跃 worker 的模块会触发 KP 框架 use-after-free panic
```

---

## 6. 编译命令速查

```bash
# KPM 模块（编译机 myserver）
ssh myserver
cd ~/wxshadow
make -C kpm clean && make -C kpm TARGET=redfin    # Pixel 5
make -C kpm clean && make -C kpm TARGET=oriole    # Pixel 6

# lab-app（macOS 本机）
sh scripts/build_lab_app.sh
```

---

## 7. 常见坑

1. **切换 TARGET 必须 clean**：两个目标共用 `kpm/build/`，不 clean 会混 `.o`。
2. **`-ffixed-x18` 不可省略**：SCS 内核，省略会 panic（见 kpm-scs-x18 skill）。
3. **`usage` 偏移不能硬编码**：SCS=y 时 `task_struct.usage` 从 0x30 偏移到 0x38，
   必须用 `stack_in_task_offset + sizeof(unsigned long)` 动态推导。
4. **卸载要先 shutdown workers**：直接 unload 活跃模块触发 use-after-free。
5. **build_lab_app.sh 路径是 macOS 的**：Linux 编译机上不能直接用，需改路径或在 Mac 上编译。
